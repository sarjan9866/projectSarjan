const { createMuiTheme } = require("@material-ui/core");

const MuiTheme = createMuiTheme({
  palette: {
    primary: {
      main: "#40B866",
      contrastText: "#fff",
    },
  },
});
export default MuiTheme;
